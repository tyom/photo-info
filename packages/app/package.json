{
  "name": "demo-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "prerelease": "pnpm version prerelease --preid=rc && git add package.json && git commit -m \"$(cat package.json | jq -r '.version')\" && git tag v$(cat package.json | jq -r '.version')"
  },
  "dependencies": {
    "debounce": "^2.2.0",
    "photo-info": "workspace:*",
    "leaflet": "^1.9.4",
    "nanostores": "^0.11.3",
    "svelte": "^5.0.4",
    "svelte-file-dropzone": "^2.0.9"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@types/leaflet": "^1.9.13",
    "vite": "^5.4.8"
  },
  "version": "0.0.1-rc.1"
}
