{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "$map-styles": ["./src/map-styles.ts"],
      "$lib": ["./src/lib"],
      "$lib/*": ["./src/lib/*"],
      "$runes": ["./src/runes"],
      "$runes/*": ["./src/runes/*"]
    }
  },
  "include": ["src/**/*.d.ts", "src/**/*.ts", "src/**/*.js", "src/**/*.svelte"]
}
