# Cloudflare deployment configuration (optional)
# Only needed if you want to store sensitive data outside of wrangler.toml

# Cloudflare API Token for deployment (optional)
# Get your token at: https://dash.cloudflare.com/profile/api-tokens
# CLOUDFLARE_API_TOKEN=your_api_token_here

# Cloudflare Account ID (optional)
# Find your account ID at: https://dash.cloudflare.com/
# CLOUDFLARE_ACCOUNT_ID=your_account_id_here

# Note: KV Namespace IDs are kept in wrangler.toml as they're not sensitive
# They're specific to your Cloudflare account and can't be used without authentication